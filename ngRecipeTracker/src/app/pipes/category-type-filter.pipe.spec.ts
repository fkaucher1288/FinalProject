import { CategoryTypeFilterPipe } from './category-type-filter.pipe';

describe('CategoryTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
